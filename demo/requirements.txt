# Gradio demo dependencies (pinned for reproducibility).
# Install into project/demo/.venv (see DEMO_REPORT.md).
#
# gradio    - the chat UI (pulls its own transitive stack).
# requests  - HTTP client for the Ollama /api/chat endpoint.
gradio==6.20.0
requests==2.34.2

# --- dev dependencies (test suite only; not needed to run the demo) ---
pytest==9.1.1
